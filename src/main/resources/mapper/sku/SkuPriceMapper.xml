<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.finalProject.ali.sku.mapper.SkuPriceMapper">
    <select id="findUnitPriceByQty" resultType="long">
        <![CDATA[
    SELECT price
    FROM sku_price
    WHERE sku_id = #{skuId}
      AND min_qty <= #{qty}
      AND (max_qty IS NULL OR max_qty >= #{qty})
    ORDER BY min_qty DESC
    LIMIT 1
    ]]>
    </select>

</mapper>
