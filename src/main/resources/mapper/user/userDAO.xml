<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finalProject.ali.user.dao.UserDAO">

    <insert id="insertUser" parameterType="com.finalProject.ali.user.dto.UserDTO">
        INSERT INTO users (user_id, password, email, name, phone, birth, address, created_at)
        VALUES (#{userId}, #{password}, #{email}, #{name}, #{phone}, #{birth}, #{address}, NOW())
    </insert>

    <select id="findByUserId" resultType="com.finalProject.ali.user.dto.UserDTO">
        SELECT * FROM users WHERE user_id = #{userId}
    </select>

    <update id="updateUser" parameterType="com.finalProject.ali.user.dto.UserDTO">
        UPDATE users
        SET
        email = #{email},
        name = #{name},
        phone = #{phone},
        address = #{address},
        profile_img = #{profileImg},
        updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <select id="findSupplierByUserId" resultType="com.finalProject.ali.user.dto.SupplierDTO">
        SELECT * FROM supplier WHERE user_id = #{userId}
    </select>

    <insert id="insertSupplier" parameterType="com.finalProject.ali.user.dto.SupplierDTO">
        INSERT INTO supplier (
        supplier_id, user_id, cp_number, cp_name, cp_phone, cp_address, status, memo, created_at
        ) VALUES (
        #{supplierId}, #{userId}, #{cpNumber}, #{cpName}, #{cpPhone}, #{cpAddress}, 'PENDING', NULL, NOW()
        )
        ON DUPLICATE KEY UPDATE
        cp_number = #{cpNumber},
        cp_name = #{cpName},
        cp_phone = #{cpPhone},
        cp_address = #{cpAddress},
        status = 'PENDING',  memo = NULL,         updated_at = NOW()
    </insert>

    <update id="updateSupplier" parameterType="com.finalProject.ali.user.dto.SupplierDTO">
        UPDATE supplier
        SET cp_name = #{cpName},
        cp_number = #{cpNumber},
        cp_phone = #{cpPhone},
        cp_address = #{cpAddress},
        updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <select id="getUserById" resultType="com.finalProject.ali.user.dto.UserDTO">
        SELECT * FROM users WHERE user_id = #{userId}
    </select>
    <update id="updatePassword" parameterType="map">
        UPDATE users
        SET password = #{password},
        updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <select id="findIdByPhone" resultType="string">
        SELECT user_id FROM users
        WHERE name = #{name} AND phone = #{phone}
    </select>

    <select id="findIdByEmail" resultType="string">
        SELECT user_id FROM users
        WHERE name = #{name} AND email = #{email}
    </select>


    <insert id="insertResetToken">
        INSERT INTO pw_reset_token (token, user_id, expiry_date)
        VALUES (#{token}, #{userId}, #{expiryDate})
    </insert>

    <select id="getUserIdByToken" resultType="string">
        SELECT user_id FROM pw_reset_token
        WHERE token = #{token} AND expiry_date > #{now}
    </select>

    <delete id="deleteResetToken">
        DELETE FROM pw_reset_token WHERE token = #{token}
    </delete>

    <select id="findPendingSuppliers" resultType="com.finalProject.ali.user.dto.SupplierDTO">
        SELECT * FROM supplier
        WHERE status = 'PENDING'
        ORDER BY created_at DESC
    </select>

    <select id="getUserRole" resultType="string">
        SELECT role FROM users WHERE user_id = #{userId}
    </select>

    <update id="updateUserRole">
        UPDATE users
        SET role = #{role}, updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="updateSupplierStatus">
        UPDATE supplier
        SET
        status = #{status},
        memo = #{memo},
        updated_at = NOW()
        WHERE supplier_id = #{supplierId}
    </update>

    <select id="findAllUsers" resultType="com.finalProject.ali.user.dto.UserDTO">
        SELECT * FROM users
        ORDER BY created_at DESC
    </select>

    <update id="updateUserStatus">
        UPDATE users
        SET status = #{status}, updated_at = NOW()
        WHERE user_id = #{userId}
    </update>

</mapper>
