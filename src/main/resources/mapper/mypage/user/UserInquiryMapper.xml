<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.finalProject.ali.mypage.user.inquiry.dao.UserInquiryDAO">

    <select id="findRecentByUserId"
            resultType="com.finalProject.ali.mypage.user.inquiry.dto.UserRecentInquiryResponse">
        SELECT
        i.inquiry_id AS inquiryId,
        i.product_id AS productId,
        i.status     AS status,
        i.created_at AS createdAt
        FROM inquiry i
        WHERE i.user_id = #{userId}
        ORDER BY i.created_at DESC
        LIMIT #{limit}
    </select>

</mapper>
