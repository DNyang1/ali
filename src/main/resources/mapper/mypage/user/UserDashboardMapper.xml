<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.finalProject.ali.mypage.user.dashboard.dao.UserDashboardDAO">

    <select id="countReadyShipping" resultType="int">
        SELECT COUNT(*)
        FROM order_item oi
        JOIN orders o ON o.order_id = oi.order_id
        WHERE o.user_id = #{userId}
        AND oi.supplier_status = 'READY'
    </select>

    <select id="countShipping" resultType="int">
        SELECT COUNT(*)
        FROM order_item oi
        JOIN orders o ON o.order_id = oi.order_id
        WHERE o.user_id = #{userId}
        AND oi.supplier_status = 'SHIPPED'
    </select>

    <select id="countActiveInquiry" resultType="int">
        SELECT COUNT(*)
        FROM inquiry
        WHERE user_id = #{userId}
        AND status = 1
    </select>

</mapper>
