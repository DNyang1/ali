<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.finalProject.ali.product.dao.SkuStockDAO">

    <update id="deductOnShip">
        UPDATE sku s
        JOIN order_item oi ON oi.sku_id = s.sku_id
        SET s.stock_quantity = s.stock_quantity - oi.quantity
        WHERE oi.order_item_id = #{orderItemId}
        AND oi.supplier_status = 'SHIPPED'
    </update>

</mapper>
